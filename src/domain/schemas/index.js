const { runtimeEnvSchema } = require('./env-schema');
const { siteConfigSchema } = require('./site-schema');
const { stayRecordSchema } = require('./stay-schema');

module.exports = {
  runtimeEnvSchema,
  siteConfigSchema,
  stayRecordSchema
};
